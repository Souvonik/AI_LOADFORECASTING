import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.company.appname',
  appName: 'AI Load Forecasting',
  webDir: 'dist'
};

export default config;
